<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="with.test.dao">
	
	<resultMap type="test" id="testResult">
		<result column="title" property="title"/>
		<result column="id" property="id"/>
		<result column="start_date" property="startDate"/>
		<result column="end_date" property="endDate"/>
	</resultMap>
	
	<insert id="registSch" parameterType="test">
		insert into withTest (
			start_date, end_date, title
		) values (
			#{startDate}, #{endDate}, #{title}
		)
	</insert>
	<select id="selectSch" resultMap="testResult">
		select start_date, end_date, title, id
		from withTest 
	</select>
	
	<update id="updateSch" parameterType="test">
		update withTest
    	set
    startDate_date = #{startDate}, 
    endDate_date = #{endDate}, 
    title = #{title}
    where id = #{id}
	</update>
	
	<delete id="delSch" parameterType="int">
		delete withTest
		where id = #{id}
	</delete>
</mapper>